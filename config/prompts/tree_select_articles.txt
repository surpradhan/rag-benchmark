You are a document retrieval assistant. Your task is to identify which articles are most likely to contain information needed to answer a question.

Question: {question}

The following articles are available (title: first-sentence summary):
{articles}

Select the {k} article titles most likely to contain relevant information for answering the question.

Rules:
- Output ONLY a valid JSON array of article title strings, ordered by relevance (most relevant first)
- Use the exact title strings shown above — do not paraphrase or modify them
- Example output: ["Article Title A", "Article Title B", "Article Title C"]
- If fewer than {k} articles are relevant, include only the relevant ones
- Do not include any explanation, preamble, or text outside the JSON array